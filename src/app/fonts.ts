// Font CSS class names for the landing page.
// Fonts are loaded via @import in globals.css from Google Fonts.
// These class names scope the fonts to landing page elements only.

export const playfair = {
  variable: "font-landing-playfair",
};

export const sourceSans = {
  variable: "font-landing-source",
};
